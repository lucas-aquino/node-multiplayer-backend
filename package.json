{
  "name": "unity-backend",
  "version": "1.0.0",
  "description": "This is a test for unity multiplayer with node backend",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only ./src/server.ts",
    "prod": "tsc && node ./build/server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.24",
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "ts-node-dev": "^1.1.8"
  }
}
